# Copy this file to .env for local runs and populate it with staging-only test credentials.
# Never commit real secrets. GitHub Actions should inject these values from repository secrets.
LOGIN_PAGE_URL=https://staging.example.test/login
LOGIN_TEST_USER=test-user@example.test
LOGIN_TEST_PASSWORD=replace-with-staging-password
